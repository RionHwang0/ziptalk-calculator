{
  "build": {
    "command": "vite build --config vite.static.config.ts",
    "directory": "dist"
  },
  "publicDir": "dist",
  "rewrites": [
    { "source": "/*", "destination": "/index.html" }
  ]
}